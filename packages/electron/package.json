{
  "name": "@tts-local/electron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'electron dev - configured in Phase 04'",
    "build": "tsc --build",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@tts-local/core": "workspace:*",
    "@tts-local/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
